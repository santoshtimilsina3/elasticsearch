<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
        'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace="com.elk.mappers.DepartmentMapper">

    <resultMap id="getDepartment" type="Department">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>
    <insert id="saveDepartment" parameterType="com.elk.model.Department">
        insert into department (id, name, created_at)
        values (#{id}, #{name}, #{createdAt})
    </insert>
    <select id="getDepartmentById" parameterType="Long" resultMap="getDepartment">
        select *
        from department
        where id = #{id}
    </select>
</mapper>